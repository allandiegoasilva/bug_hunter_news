name: bug_hunter_news
description: Bug Hunter news app to see all new bug bounty programs

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15
  flutter_launcher_icons: ^0.10.0
  splashscreen: ^1.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

assets: 
 - assets/SplashScreenLogo.png

flutter:
  uses-material-design: true

